// ES7 has two additions in the language itself:

// .includes(); // method on Strings and Arrays

const pets = ['dogs', 'cats','birds'];

pets.includes('dogs'); // true, because it includes dogs in that particular array
pets.includes('snakes'); // false, because that array doesn't include thatvalue in it.



// exponentional operator
const square = (x) => x ** 2; // kvadratura (x to the power of two)
const cube = (y) => y ** 3; //  kubni